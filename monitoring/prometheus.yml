global:
  scrape_interval: 15s
  evaluation_interval: 15s


scrape_configs:

  # ========================================================
  # ClinicFlow FastAPI Backend
  # ========================================================

  - job_name: "clinicflow-backend"

    metrics_path: /metrics

    static_configs:
      - targets:
          - "backend:8000"